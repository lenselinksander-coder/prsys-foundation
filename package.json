{
  "name": "prsys-foundation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix web && npm install --prefix backend",
    "build:web": "npm run build --prefix web",
    "start": "NODE_ENV=production npx --prefix backend tsx backend/src/server.ts",
    "dev": "concurrently \"npm run dev --prefix web\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5"
  }
}
