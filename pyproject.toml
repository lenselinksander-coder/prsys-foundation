[project]
name = "prsys-foundations"
version = "0.1.0"
description = "Minimal pre-governance load-capacity model (hackathon prototype)."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
prsys = "prsys.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
